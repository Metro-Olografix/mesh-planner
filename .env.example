# Copy to .env and fill in the values

# PostgreSQL
POSTGRES_PASSWORD=change_me_in_production

# Zitadel OIDC — injected into the frontend at runtime, no build args needed
ZITADEL_DOMAIN=https://auth.example.com  # no trailing slash
ZITADEL_CLIENT_ID=your_zitadel_client_id

# Logging: DEBUG | INFO | WARNING | ERROR (default: INFO)
LOG_LEVEL=INFO
