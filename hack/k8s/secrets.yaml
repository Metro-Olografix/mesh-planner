# Template — do NOT commit real values.
# Create the secret from the command line:
#
#   kubectl create secret generic mesh-planner-secrets \
#     --namespace mesh-planner \
#     --from-literal=POSTGRES_PASSWORD=<value> \
#     --from-literal=ZITADEL_CLIENT_ID=<value>
#
# Or use a Secret manager (Sealed Secrets, External Secrets Operator, etc.)
apiVersion: v1
kind: Secret
metadata:
  name: mesh-planner-secrets
  namespace: mesh-planner
type: Opaque
stringData:
  POSTGRES_PASSWORD: change_me
  ZITADEL_CLIENT_ID: your_client_id
